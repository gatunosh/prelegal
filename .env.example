# Copy to .env and adjust. All values are optional for local development.

# Secret used to sign JWTs. Set a long random value in any real deployment.
JWT_SECRET=dev-insecure-secret-change-me-0123456789abcdef

# OpenRouter API key (used by future AI features, not by the V1 foundation).
OPENROUTER_API_KEY=
